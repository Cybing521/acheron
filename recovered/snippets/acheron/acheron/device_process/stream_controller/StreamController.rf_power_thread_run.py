# Source Generated with Decompyle++
# File: tmp0vsq4kcr.marshal (Python 3.11)


try:
    if self.disconnected.wait(1):
        self.device_lock
        self.logger.debug('Relinquishing control of RF Power')
        self.device.reset_rf_power_timeout(1)
    else:
        except asphodel.AsphodelError:
            pass
        None(None, None)
        return None
    with None:
        if not None:
            pass
    return None
    
    try:
        self.device_lock
        self.device.reset_rf_power_timeout(5000)
        
        try:
            None(None, None)
        with None:
            if not None:
                
                try:
                    
                    try:
                        continue
                    except Exception:
                        self.logger.exception('Uncaught exception in rf_power_thread_run')
                        
                        try:
                            pass
                        try:
                            self.device_lock
                            self.logger.debug('Relinquishing control of RF Power')
                            self.device.reset_rf_power_timeout(1)
                        except asphodel.AsphodelError:
                            pass

                        None(None, None)
                        return None
                        with None:
                            if not None:
                                pass
                        return None
                        self.device_lock
                        self.logger.debug('Relinquishing control of RF Power')
                        self.device.reset_rf_power_timeout(1)
                    except asphodel.AsphodelError:
                        pass

                    None(None, None)
                    with None:
                        if not None:
                            pass




